# v5.15 — THE BOY (James re-voiced as a twelve-year-old, and Chad's sheet edits)

Chad's ask, verbatim:

> lets go with valf. I've made very extensive edits to the text and
> voicelines here on this sheet. Make sure you comprehensively cover
> everything, i made many changes in the entire sheet. Not just the
> voicelines. Make sure you plan on how to go about doing this, as the
> context might get compacted halfway. You need to have proper progress
> tracking, checkpoints, record memory along the way to avoid context loss.
> Do it carefully, don't introduce new bugs, break anything or accidentally
> remove anything or create any regressions.

**This file is the memory.** If the session's context is compacted or lost,
resume from the LOG at the bottom: it says which checkpoint was last
committed and what the next step is. Every checkpoint is a commit whose
message begins `v5.15 CPn`. Audio progress has its own record,
`masters/v5.15/progress.json` (committed with each batch), so no take is
generated twice and no take is forgotten.

## The decision

The main character's voice becomes **VALF** (`loY1uopAz31XyhAEhNSa`,
"Child, Young, Playful & Sarcastic", eleven_v3) — Chad's pick after three
rounds of samples (VALF/Teddy/Toru/Aaron; Johnny/River/Edison/Ahmed/
Gabriel; then five-line range tests of VALF, River and Johnny). The
workspace has no Chinese or Singaporean child voice; VALF is the only
unmistakable child. The voice id changes in ONE place —
`SPEAKERS.james.voice` in `src/voicelines.js` — and every row marked
`james` is regenerated.

## The plan, as checkpoints

- **CP1 — the words.** Read Chad's sheet back (GAME TEXT v19, id
  `1Mo0swFP8_mtJAoFK3Zv1LepT_TBuKj3b1zuAPrFkbbU`), save the markdown to
  the scratchpad, `node textsync.mjs import`, and REVIEW the whole diff of
  `src/strings.js`, `src/chapters/*.js`, `src/voicelines.js`: placeholders
  (`{chapter}`) intact, `<b>`/`<br>` intact, no cell emptied by accident,
  keycaps still short. `chaptertest` + `texttest` green. Commit.
- **CP2 — the takes.** Switch the speaker id. Regeneration list = every
  `james` row (all 72, the two unused ones included, so no old-voice file
  lingers) + every other-speaker row whose text changed (the import names
  them). One generation per line, eleven_v3, the registry text as the
  prompt, direction tags from the row's note (Shouted → `[shouting]`,
  Whispered → `[whispering]`). eleven_v3 fails about one line in three at
  random — re-run unchanged, never rewrite. Masters land in
  `masters/v5.15/<id>.mp3`; `progress.json` records each line's state.
- **CP3 — the encode.** Match the shipped contract exactly (mono voice,
  44.1 kHz, 128 kbps mp3, `-map_metadata -1`; Opus 48 kHz 64 kbps mono
  into `assets/audio-opus/`), level-matched against the current James
  takes by `volumedetect`. Chapter 1's `voice` is its own asset
  (`assets/voice.mp3`). Re-measure every `secs` in the registry.
- **CP4 — the timing.** A child says a line at a different speed. Static
  overlap scan of every film and scene in all five chapters: each voice cue
  with its NEW length; flag a voice cue that starts before the previous
  voice cue ends, and any cue past the scene's end. Shift only what must
  move; camera beats stay. Probe with `cinetest`.
- **CP5 — the release.** Build; full suite in batches; docs (this file,
  CLAUDE.md entry, AUDIO-PLAN, EDITING-TEXT, LEARNINGS if anything was
  learned); sheet **v20** exported and uploaded (lengths and words changed,
  so v19 is stale the moment CP3 lands), read back and round-tripped;
  VERSION 5.15; deploy from inside `dist/`; verify; bundle refresh (a
  whole-cast re-voice is a milestone).

## Rules in force

- Never delete by line-range; targeted edits; read the full diff before
  every commit and account for every removed line.
- The sheet is Chad's: every word he changed is applied as written. Where
  a change breaks something mechanical (a placeholder, a tag), fix the
  mechanism and tell him, do not silently revert his words.
- Cutscene timings move only where a take no longer fits.
- Both builds green; generated files never hand-edited.

## LOG

- (CP0) plan written, tasks created. Next: read the sheet back and import.
- (CP1) Chad's v19 sheet read back to `scratchpad/sheet-v19-chad.md`
  and imported: 137 UI strings (5 changed: credits footer, the selector's
  hint, Worn→Equipped, Carried→Bag, the desktop drag hint, the phone's
  torch→light source), 110 chapter strings (ch2/ch3/ch4/ch5: choices,
  outcomes, teachings; ch2 and ch3 `core` lost their Pali second line by
  his choice; ch5 D is now "Ask him to settle the presence."), 24 voice
  lines changed. Bug found and fixed on the way: `writeStrings` skipped
  every key starting `ch` as a chapter's, so the selector's `chapters.*`
  strings had been un-importable since v5.12 — now `ch\d+`. `chaptertest`
  and `texttest` green. The 24 changed takes: voice vpile v2near v2gap
  v2B v2C v3wake2 v4voice v4thinkA1 v4regret v5wake2 v5wake3 v5voice
  v5near v5fearB1 v5B v5C (James, regenerated anyway) + v3aunt5 v3aunt3
  v3aunt4 (auntie) + v4ma2 (Ma) + t5teachA t5fearB t5disC (tang-ki).
  Next: CP2 — switch the speaker id, generate.
