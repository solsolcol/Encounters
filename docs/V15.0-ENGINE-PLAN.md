# v15.0 — THE ENGINE PASS (plan, checkpoints, and the record)

This file is the memory of the v15 work. Every checkpoint writes here before
it moves on, so a session that loses its context can pick up from the last
line marked DONE.

## 0. The ask (Chad, during v14.16, verbatim)

> After you are done with all these and deployed what you were working on,
> next i want you to think of how to dramatically optimize the game without
> resorting to compromising visual quality or fidelity in any way. It has to
> be technically sophisticated, like increasing the multi threading
> capabilities, improving the game engine, low level optimizations, engine
> optimizations, reducing overhead, improving memory management, etc, all of
> these highly technical improvements that are objectively superior. Think of
> what even i have not thought of. Plan it out, have steps and milestones and
> checkpoints, save memory along the way to avoid context loss and memory
> compaction, keep executing continuously until you are fully done and deploy
> it.
>
> You should not resort to cheap methods that may have unintended effects,
> like compressing things, reducing resolution, etc. They should be real
> technical improvements to the core engine.

## 1. The rules this work is held to

1. **Zero visual change.** The same pixels, proven by a pixel diff of fixed
   camera poses before and after (tolerance 0 unless a float-level
   difference is explained). No resolution or pixel-ratio change, no mesh or
   texture compression or decimation, no LOD that swaps visible detail, no
   smaller shadow maps, no fewer lights, no lower frame cap.
2. **Zero behaviour change.** Timings a cutscene depends on, sounds, save
   format, harness contracts — all identical. The 24 harnesses stay green.
3. **Measured, not assumed.** Every change has a before and an after number
   from the same probe on the same box (`dbgprof.mjs`, §3).
4. **Both builds.** The strict-CSP single-file build keeps working; anything
   that needs a real worker file degrades to today's path there.
5. **iPhone first.** Chad plays on an iPhone 13 (iOS Safari, WebGL2). A
   feature Safari lacks must fall back to today's path, never to less.

## 2. Milestones

| id | what | status |
|---|---|---|
| M0 | Baseline profile of all ten chapters (`dbgprof.mjs`), code survey (workflow `v15-engine-survey`) | IN PROGRESS |
| M1.. | set from the survey and the profile (§4) | — |
| MZ | full suite, pixel-identity proof, release, deploy, byte-verify | — |

## 3. How it is measured

`dbgprof.mjs <label> <chapters…>` — for each chapter: into play, six seconds
to settle, then twenty seconds with the view turning 0.2 rad a second:

- CDP `Performance.getMetrics` deltas per second of play: script, task,
  layout and style-recalc milliseconds, and layout/style counts;
- a CPU sampling profile (0.5 ms): self time per function, the garbage
  collector's share, idle;
- the renderer's own counters summed over BOTH passes of every frame (the
  world and the viewmodel): renders, draw calls and triangles per frame;
  programs, geometries and textures resident; the JS heap.

Output `prof-<label>.json`. SwiftShader draws on the CPU in the GPU process,
so frame RATE on this box says little about a phone's GPU; the main-thread
numbers (script, GC, layout, style) are the engine's own overhead and are
what these changes are judged on, with draw calls and state changes as the
GPU-side proxy.

## 4. The plan

The survey (a read-only workflow over the engine and all ten chapters, then
synthesised against the baseline) is the source. What it proposed, what was
kept, and why:

- **M1 — prove and close CP7–CP10, release v15.0.** Plus four closures the
  review found: the viewmodel's matrices kept fresh when its pass is skipped,
  the letterbox 2 CSS px into each bar (the scissor is in buffer pixels,
  rounded), the matrix skip sign-exact at zero, and every warmed light state
  DRAWN once into one pixel (a driver may build a pipeline at its first draw,
  not at link). Folded in, because each is exact and small: a skeleton that
  did not move is not re-uploaded (M2.1), the shared tree kit is not
  re-uploaded per stand (M5.5), the warm draw under the curtain is one pixel
  (M5.3). The title stays a covered frame (its layer is an opaque gradient
  over the whole viewport); `leaktest` lifts it instead, which keeps the
  engine's own frames the thing it counts.
- **Later releases, in order of gain over risk**: parse each episode-2
  character once (e2c1 holds eight duplicate 2048² textures, ~179 MB of GPU
  memory), the ghost detached before a chapter's sweep so she is not freed and
  re-uploaded at every chapter change, the loaders' caches holding the scene
  rather than the whole parser, decoded sound held only while it can sound.
- **Not done, with reasons** (the survey's §4): batching static props (not
  pixel-identical), SharedArrayBuffer/COOP-COEP (blocks the font sheet and can
  never serve the single-file build), OffscreenCanvas workers (iOS 17+ only),
  suspending the audio context while muted (freezes tails), lazy mixers (a
  hand-kept list of every bone reader). Threads: meshopt already decodes in
  two workers, audio decodes on WebKit's decoder thread, shaders compile in
  the background where the driver allows. What heats a phone is total joules,
  mostly the GPU — moving work to another core removes none of it; deleting
  work does, which is what every change here does.
- **Chad's decisions** (not assumed): the skeleton merge for episode 1's
  ghost and chapter 3's files (touches base-game files; pixel-identical but
  his call), recovering after iOS drops the graphics context, self-hosted
  fonts, a shorter loading card on a fast phone.

## 5. Record

- CP0 — v14.16 shipped (869da6a, deploy 6ab6faa87189d8aed14df67e). v15
  starts from it. `dbgprof.mjs` written; baseline in `prof-base1416.json`.
- CP1 — THE BASELINE, read. This box renders on SwiftShader (0.1–1.1 fps),
  so main-thread script is tiny next to the GPU: 2–5 ms of script per
  second, 5–15 ms per frame. The GPU proxy is the number that matters, and
  `dbgtris.mjs` (the scene frustum-culled exactly as three does, triangles
  summed by subtree) found where it goes: in e2c3's harbour **1.09 M of the
  1.1 M triangles a frame are TREES**, in all four directions, because an
  InstancedMesh is culled as a whole and a stand surrounds the player; in
  ch3 the trees are 234 k and the seated crowd 255 k (70 skinned meshes with
  `frustumCulled = false`, drawn whichever way the player looks).
- CP2 — **M1 INSTANCE CULLING, DONE.** `cullEachInstance` / `cullInstances`
  in main.js: each tree stand's full instance list is kept, and before the
  world is drawn the instances whose own sphere is in the frustum are packed
  to the front in their original order; a shadow-map frame draws the full
  list. Switch `OPT.instCull`. Proven by `dbgpix.mjs` (the same frozen frame
  drawn off and on in one task, every pixel compared, eight headings):
  **identical at every heading**, e2c3 1.09–1.39 M → 0.32–0.95 M triangles
  (31–71 % fewer), ch3 18–24 % fewer.
- CP3 — **M2 SPHERE CULLING, DONE.** `cullBySphere(root, cx, cy, cz, r)`
  (engine, and a CHCTX seam): a root three cannot cull (a skinned clone posed
  from a skeleton elsewhere) is taken off the camera's LAYERS while a
  generous sphere round it is outside the frustum — not its visibility (the
  chapter's) and not its boundingSphere (three's sort key). Shadow frames and
  the warm frame restore everything. Chapter 3's seated crowd registers each
  sitter (1.6 m about his middle; he fills 1.30 m by ~0.6). Switch
  `OPT.sphereCull`. `dbgpix.mjs sphereCull ch3`: **identical at all eight
  headings**; facing away from the tent a further 59–63 % fewer triangles and
  about half the draw calls (on top of M1); facing the crowd, unchanged.
- CP4 — **M3 SHADOW TRIM, DONE.** `shadowCasterSync()` under the curtain
  (before the compile) and every 120 frames: a light declared to cast turns
  its shadow off while the world holds NO caster (hidden meshes counted), and
  back on the moment one appears. Measured on a phone (390 × 844, touch): all
  five episode-2 chapters have **0 casters** (everything is
  `castShadow = !LOW`) while 95–493 receiving meshes sampled the moon's empty
  map — nine PCF compares per lit pixel for a map that always answers "lit".
  `PHONE=1 dbgpix.mjs shadowTrim e2c1 e2c5`: **identical at all sixteen
  headings**; e2c1 on the phone now reports no light casting; ch1 (97
  casters) keeps both of its shadow lights. Switch `OPT.shadowTrim`.
- CP5 — **two exact CPU trims.** `darkLights()` walks a LIST of the lights
  (refreshed every 30 frames and on every stage rebuild) instead of
  traversing the whole scene graph every frame to find ~11 of them; every
  light it ever hid is in the list because it was in it when hidden (and the
  loop's early-out is `continue`, not the traversal callback's `return`).
  `loopVol`/`loopPan` re-aim an AudioParam only when the target MOVES: an
  exponential approach re-aimed at the same target is the same curve, so a
  repeat only queued another automation event for every bed, every frame.
- CP6 — **binary sound packs, DONE and verified.** build.py writes each pack
  twice: the JSON-of-base64 the single-file build inlines, and for the hosted
  build a binary one ('MZP1', a JSON index, the files byte for byte). The
  engine reads either (`packParse`); a binary pack's sounds are views into
  one buffer, copied only when one is decoded. Netlify already brotli-
  compresses the JSON in transit (4.82 MB over the wire for 6.43 MB), so this
  is NOT a smaller download and is not claimed as one: it is a 6 MB JSON.parse
  and every per-sound base64 decode taken off the phone's main thread, and a
  quarter less memory held for sound all session (heap at e2c1 after the
  journey 133 → 89 MB, uncollected). All 1,177 sounds checked byte for byte
  against their source files and the JSON packs: 0 mismatches. csp, hosted,
  sound, menu green; `dbgsound16.mjs` unchanged (every opening line, every
  loop retired, the chair scrape a sample).
- CP7 — **covered frames, the letterbox, the empty hand pass — coded;
  letterbox PROVEN.** `worldCovered()` (title, the chapter card once forced
  solid, a film/scene held on black, the episode card): nothing is drawn;
  the logic runs, the matrices are brought up to date, shadow redraws wait
  for a drawn frame, the warm frames are forced. `letterboxArm()`: once both
  bars measurably cover their edges, both passes are scissored to the band
  between them (1 px into each bar); off before the bars leave.
  `dbgletter.mjs`: band = 78 % of the height in ch1, ch3 (desktop) and e2c1
  (phone), **identical inside the band** at three film times each. The hand
  pass is skipped when no mesh under the hands is visible. The suite already
  runs visibly faster (census 58 → 41 s, csp 69 → 52 s): the software
  renderer no longer draws under the title and the cards.
- CP8 — rescueTextures looks before copying (csp green: the strict build's
  rescue still works); the download ahead drains the stream instead of
  building each file as one buffer.
- CP9 — **in-play shader compiles, measured and fixed (to be re-measured).**
  `dbgfirstsight.mjs`: HER FIRST APPEARANCE in ch1 compiled **8 programs
  mid-play** (her light going up changes the point-light count, which is part
  of every lit shader) and the worst frame was 8.6 s on the probe box.
  `warmLightStates()` compiles, under the curtain, the states the engine's own
  lights reach: her light on while she fades (transparent) and when solid,
  the torch on, the muzzle flash on; three keeps every variant per material,
  so the moment in play is a lookup.
- CP10 — **matrix skip, coded.** `Object3D.prototype.updateMatrix` remembers
  the ten numbers and the parent it composed from; bitwise the same → no
  recompose and no dirty flag, so a subtree where nothing moved costs one
  comparison per node instead of a compose and a forced multiply. Proof to
  run: `dbgmat.mjs` (incremental vs three's forced full recompute, bitwise,
  in films and in play).
- CP11 — **light-state warm-up PROVEN.** `dbgfirstsight.mjs` again: her
  first appearance in ch1 now compiles **0** programs mid-play (it was 8);
  79 programs resident (12 variants compiled under the curtain); the worst
  frame of the 20 s window 8.6 s → 4.1 s on the probe box (what is left is
  drawing, not compiling), 26 frames drawn in the window against 10.
- CP12 — **matrix skip PROVEN, after one real bug.** The first version
  (composed-numbers check only) left the CAMERA stale in films: three r185's
  `updateWorldMatrix` refreshes a child only when the child is flagged, and
  stock three.js survives that because every object flags itself every
  frame. `dbgmat.mjs` caught it (102 stale matrices in ch1's film against a
  forced full recompute). Fixed by STAMPS: every world-matrix computation
  takes a sequence number and records its parent's; a node recomputes when
  flagged, forced, or its parent's stamp moved. `dbgmat.mjs ch1 e2c1 ch3
  e2c4`: **0 matrices differ** in films and in play (683 / 3,664 / 2,172 /
  512 objects, sampled repeatedly while things move). LEARNINGS gets the law.
- CP13 — `leaktest` lifts the title before it measures: the world is not
  drawn under an opaque cover any more, and the harness counts what drawing
  uploads (a harness assumption, not a game change).
- CP14 — **M1 CLOSED, and three exact savings folded in.** The viewmodel's
  matrices are brought up to date when its pass is skipped and on covered
  frames; the letterbox band is 2 CSS px into each bar; `matSkip` compares
  sign-exact (`eq`: === and, at zero, the sign); each warmed light state is
  drawn once under a 1×1 scissor. `firstsight.mjs` after: **0 programs made**
  at her first sight and the worst frame **2.0 s** (8.6 s before CP9, 4.1 s
  after it) — the pipeline build was the rest of the stall. Folded in:
  `OPT.boneSkip` (Skeleton.update compares as float32, bit for bit, and flags
  the texture only when a bone moved), the tree kit's anisotropy set once
  (every stand used to re-upload and re-mip the kit's shared sheets),
  `OPT.warmTiny` (the curtain's warm draw into one pixel). The probes are
  TRACKED now: `tools/probes/` (README there), fixing the survey's R1 — every
  "proven" line above can be repeated after a container reset.
- CP15 — **coded, under measurement: parse once, the ghost kept, caches
  lightened, the rescue shared, the settled light state.** e2c1 parses each
  character ONCE (`parseOnce`; every crowd copy and rig a clone of the
  untouched original; `ghostify` clones materials first so the sergeant is
  never ghosted by the three men who share his file). `rebuildStage` takes the
  ghost out of the world BEFORE the chapter's sweep (she was freed and
  re-uploaded at every chapter change). e2c3/e2c4/e2c5's parse caches resolve
  `{scene, animations}`, not the parser with its copy of the binary chunk.
  `rescueTextures` (the strict-CSP build) makes one texture per image BYTES,
  as three's own loader does (hdb.glb lists one buffer view fourteen times).
  `warmLightStates` compiles on top of the SETTLED light set (darkLights' end
  state) and skips a state whose light counts are already compiled. `?opt=`
  in the address switches any OPT off, so a load-time switch can be compared
  build against itself. BEFORE (`tools/probes/gpumem.mjs`, this box): e2c1
  194 textures, 394.7 MB of distinct images, 23 parses, 10 bone-texture
  uploads a frame; ch3 206 textures, 199.7 MB, 27 parses, 56 uploads a frame.
- CP16 — **v15.0 RELEASED.** AFTER (`tools/probes/gpumem.mjs`): e2c1 textures
  194 → **186** (exactly the eight duplicate sheets), distinct texture images
  76 → 68, **394.7 → 224.1 MB**, parses 23 → 15, programs unchanged at 81;
  ch3 unchanged in memory (it had no duplicate parses), programs 55 → 56. Bone
  uploads unchanged in the two chapters measured (10 and 56 a frame) — every
  skeleton visible at the spawn animates; `boneSkip` was unit-checked against
  three's own `update` (identical numbers; no flag when nothing moved) and
  pays in the films' parked crowds. `dbgshare`: the sergeant keeps his own
  opaque material, each of the three ghosts its own transparent copy, one
  2048² sheet shared by all four; 48 botak copies share one material. `mat.mjs`
  with the sign-exact compare: 0 differ (ch1, e2c1, ch3, e2c4, film and play).
  `progs.mjs`: e2c4's film and play compile NOTHING after the curtain; e2c1's
  film compiles 25 at four cuts (12.5 s +1, 21.5 +5, 28.5 +9, 70.0 +8, 76–77
  +2) — the film's SETS carry their own lights, so the light counts change at
  each cut. That is v15.1 (the patch is written: a hidden group holding a lit
  light is a warm state, and so is one dark lamp of each kind coming on).
  **Full suite 24/24 in 1772 s** (v14.16's took 2794: the software renderer
  no longer draws under the covers).
- CP17 — **v15.0 DEPLOYED** (`6ab72afd8a010dab98a15c3a`, 107 files
  byte-verified; the v15.0 zip was later overwritten by a WIP build and
  rebuilt from 0d8daac in a worktree — its index.html byte-identical to the
  live one). **v15.1 in progress** (VERSION 15.1):
  - **Film light states** (`OPT.lightSets`): every light not drawn at the
    curtain is put with the others of its GROUP; each group, two sibling
    groups at once (a crossfade), the room's dark lamps all at once and one
    dark lamp of each kind are warm states; her states also where a film
    shows her (`ghostOpacity(` in the chapter's text). `progs.mjs` (lightSets
    on/off, same build): e2c1 film 30 → 0 mid-film compiles, ch3 8 → 0,
    ch1 44 → 22 (the rest is the pocket crossfade — sibling pairs added
    after), ch4 6 → 6 (its film turns the flat's lamps OFF — see lightMem).
    Cost: e2c1 compiles 119 → 201 programs under the curtain.
  - **Light memory** (`OPT.lightMem`): a frame after the curtain that
    compiled a program records its light COUNTS (per chapter, per LOW) in
    `mz.encounters.lightsets`; the next curtain reproduces each with
    stand-in lights at intensity 0 (or by hiding real ones; casters only
    with real lights) and compiles it. `REPEAT=1 progs.mjs` proves it.
  - **v15.0 defect found and fixed**: `glerrors.mjs` counted **257 rejected
    draws** (GL_INVALID_OPERATION, shadow sampler mismatch) under ch1's
    curtain — bisected by `?opt=` to `coverSkip`: with covered frames not
    drawn, the warm draw is the FIRST draw, before any shadow map exists, and
    three's 1×1 stand-in depth texture is never uploaded (DepthTexture has
    version 0). Invisible (all under the cover) but the warm draws failed.
    `warmGeometry(..., shadows)` draws the maps in the same pass when a drawn
    light has none; `redoShadows()` after the warm makes them true on the
    covered frames.
  - **Lost context** (`OPT.ctxRestore`): the room environments (PMREM, GPU
    only) regenerated on all three renderers and the shadow maps redrawn on
    `webglcontextrestored`. `ctxloss.mjs` drill written; its first run
    crashed the probe browser (to re-run alone).
  - **Her sounds** (`OPT.herSounds`): in a `ghost: null` chapter that names
    none of them, the warm lists stop decoding her 13 sounds (~21 MB) and
    entering such a chapter lets them go; `whenDecoded` waits only for what
    will decode (`warmable`) — which also removes a latent 4 s wait when a
    film's bed names another chapter's sound.
  - **Music park** (`OPT.musicPark`): where a chapter declares `musicVol: 0`
    (ch3, all of episode 2) the explore music stops 3 s in and its 46 MB go;
    a chapter with music decodes it again and fades it in from the top.

- CP18 — **the regression audit** (Chad: "make sure the previous few
  versions' fixes and improvements … were not accidentally undone"). A
  workflow read every claim v14.7–v14.16 makes (the amulet's flow and ward,
  the rarity and crack/break banner, the soldier figure and `ZAV_MIPS`, the
  full-detail amulets and their LOD, the bag loaded at boot, the curtain,
  the meshopt workers, the download ahead, every v14.16 fix) against the
  v15.1 source, and checked every v15 change adversarially. **Every earlier
  claim is intact.** Eight findings were confirmed and are fixed (fcda020):
  the sphere-cull set kept a disposed chapter's culled roots (they keep
  their parent, the old world group), so it is emptied at every rebuild and
  detached tree stands leave the instance set with it; a model whose
  PLACEMENT throws in a promise (episode 2's parse-once caches) was a
  console warning only — `ctx.loadFail(key, err)` logs it beside the loads,
  where walktest fails on it (once: a file that failed to load is already
  logged); her light was a "group" warm state in every chapter (it lives in
  the ghost), compiled for nothing where she cannot appear — she has her
  own states; her two LOOPS kept running at gain 0 after her buffers were
  released in a chapter without her (a source holds its own buffer) — they
  are retired as every chapter's beds are (v14.16); a session that STARTS in
  a chapter with its music parked cut the music in at full on the first
  chapter that has music — it fades in, as a return from a park does; and
  the header comment still described a warm state v15.1 had removed. For
  the record against CP17: 8bfb302 dropped the "every dark lamp at once"
  state (no film reaches it — measured — so it was only curtain time), and
  the camera's own lights (torch, flash) and HER light are never grouped:
  each has explicit states. What no rule guesses, the light memory learns
  and the shipped seeds (c2230ee, `src/lightseeds.json`) carry.
- CP19 — **the seeds, recorded and measured.** `tools/probes/seedlights.mjs`
  played every film and every ending of all ten chapters on a phone profile
  (390 px, touch — LOW) and a desktop one (560 × 350: the counts do not
  depend on the window, and the software renderer pays for every pixel),
  and the engine's own light memory kept every count it had to compile on
  screen: 37 states, now `src/lightseeds.json`. Then the same probe on a
  FRESH profile against the build that ships them (on-screen compiles,
  before → after): chapter 4 film + scenes 18 → 1 (desktop) / 17 → 1
  (phone); episode 2 chapter 2's film 39 → 4 / 29 → 0; episode 2 chapter
  4's film and endings 42 → 4 / 43 → 1. What is left is one to three programs at a
  scene's first frame — not light states (the seeds hold every count those
  frames reached), and not worth a guess. The cost is curtain time: chapter
  4 compiles 178 programs under its cover instead of 120, e2c2 182 instead
  of 109.
- CP20 — **v15.1 RELEASED** (edb5c0a): full suite 24/24 in 2331 s, deploy
  `6ab78f47ee525732110c536d`, byte-verified (index.html and all 107 build
  files identical on the live site).
