# v13.0 · CHAD'S TWENTY-TWO — the plan, the checkpoints, the record

Chad, 18 Sep 2026, one message, 22 numbered items across the episode-2 title,
all four built chapters and the weapon system. His instruction on process:

> You must execute all of these feedback flawlessly, without introducing any
> bugs, break anything, cause any regressions, do it cleanly, make sure they
> are all done to the fullest effort with no sloppiness, have checkpoints,
> milestones, record progress, record the plan, steps, so that you survive any
> potential memory loss, context loss, or context compaction midway through
> your edits. Everything must be done 100%.

**THIS FILE IS THE MEMORY.** Every checkpoint below is committed as it lands
and its row in §3 is ticked in the same commit. A session that resumes with no
context reads §3 first, finds the first unticked row, and continues there.

Standing rules that bind this work (CLAUDE.md): never delete code by
line-range; read the full diff before every commit and account for every
removed line; episode 1 is the frozen v6.17 reference and must stay untouched
by construction; the DEPLOY comes before the sheet; test proportionately.

---

## 1 · The twenty-two, verbatim, with where each one lives

| # | Chad's words (abridged) | Where |
|---|---|---|
| 1 | Episode 2's overall title → **'The 3 AM Shower & The Ghost Cyclist'** everywhere, including the chapter selector | `src/strings.js` `ep2.title` |
| 2 | Every episode-2 chapter's intro TITLE CARD follows the actual title — "I dont know why you added night one, two, etc" | `cardTitle` in e2c1–e2c4 |
| 3 | The life buoy at the 'WELCOME TO PULAU TEKONG' terminal floats in the air — tie it to the ledge | e2c1 film set |
| 4 | e2c1: remove the black boots by each bunk, remove the green box on top of each locker | e2c1 |
| 5 | e2c2: talking to a bunkmate TWITCHES his body and cuts his legs into the bench | e2c2 |
| 6 | e2c2: "the encik" label still appears near him — no real purpose | e2c2 pile words |
| 7 | e2c3: "something was pressing" needs to sound more scared | `n3press`, ElevenLabs |
| 8 | e2c4 film: start from the RANGE panning, not the truck — a phone sees nothing | e2c4 intro |
| 9 | e2c4: the live-rounds props look like nothing | e2c4 world |
| 10 | e2c4: put the encik at the table with the live rounds | e2c4 world |
| 11 | e2c4: when the scene lights up, SHOW the flare going up, with sound — film AND play | e2c4 both |
| 12 | e2c4 film: end at the lane-6 pan; no second live-rounds view | e2c4 intro |
| 13 | e2c4: REMOVE the reload and ammo-count systems — unlimited ammo, no reload | engine `weapon` |
| 14 | e2c4: AIM button as big as FIRE and labelled **AIM**, not an icon | engine HUD |
| 15 | e2c4: aiming ZOOMS with an animation; use the gun's own aim take if it has one | engine weapon |
| 16 | e2c4: the watchtower looks incomplete; put an FBO soldier on top of it | e2c4 world |
| 17 | e2c4: targets stand before shooting begins — all DOWN until a wave starts, DOWN when it ends | e2c4 serials |
| 18 | The LOAD drill's brief needs a looping demo animation (like the standby bed), shorter text; on desktop left-click fires the gun while playing it; check mobile | engine event + e2c4 |
| 19 | e2c4: first wave static, then straight to the MOVERS — drop the second static wave | e2c4 serials |
| 20 | e2c4: "something is out there" appears before the cyclist does — gate it, and give it its own sting | e2c4 + sound |
| 21 | e2c4: the cyclist FADES in and out, and is BIGGER on the left-to-right pass; an aimed-in player may never see him | e2c4 |
| 22 | e2c4: when the cyclist first appears EVERYONE SHOUTS — different lines per man | e2c4 + ElevenLabs |

## 2 · What is NOT in scope, deliberately

- Episode 1 (ch1–ch5). Item 2 names episode 2 only; episode 1's card subtitles
  are evocative lines ("I Should Never Have Taken"), not the location stamps
  Chad objects to. Items 13/14/15 reach the engine's `weapon` block, which
  episode 1 never declares — untouched by construction.
- The chapter-selector layout. Item 1 is one string; v6.5's layout stands.

## 3 · CHECKPOINTS — the resume table

Tick a row only when its work is committed. `—` = not started.

| CP | What | Items | State |
|---|---|---|---|
| CP0 | This plan doc committed | — | **done** |
| CP1 | Titles: episode 2's name, four card titles | 1, 2 | **done** |
| CP2 | The small world fixes | 3, 4, 5, 6 | **done** |
| CP3 | Sound: `n3press` re-said, the flare, the ghost sting, the shouts | 7, 11a, 20a, 22a | **done** |
| CP4 | e2c4 film: open on the range, end at lane 6, the flare shown | 8, 12, 11b | **done** |
| CP5 | Weapon: no reload, no ammo count, AIM button, zoom animation | 13, 14, 15 | **done** |
| CP6 | e2c4 world: rounds props, the encik at the table, the watchtower | 9, 10, 16 | **done** |
| CP7 | e2c4 serials: targets down between waves, the second static wave dropped | 17, 19 | **done** |
| CP8 | e2c4 the cyclist: gated banner + sting, fade, size, the shouts | 20, 21, 22 | **done** |
| CP9 | The LOAD drill: demo loop, shorter text, the click that fired the gun | 18 | **done** |
| CP10 | RELEASE: build → harnesses → commit/push → DEPLOY → byte-verify → docs | — | **in flight** |

## 4 · The record — what each checkpoint actually did

### CP10 · the release

VERSION 13.0. Sheet **v64** exported to `masters/v13.0/masterz-text-v64.xlsx`
(four tabs: UI TEXT 275, EPISODE 1 110, EPISODE 2 153, VOICE LINES 256) and
NOT published — the workbook is past the Drive connector's base64 wall and the
split export is still to do, so **v44 stays the link to give Chad**.

**Two numbers were chosen rather than measured during the work, and both were
WRONG until the render said so** (the record matters more than the fix):

1. The AIM's weapon offset was ADDED to the rest. On a portrait phone
   `layoutHands` has already nosed the rifle inward (v12.0), so the two
   stacked: photographed at 390 px the weapon sat at x −0.2028 with its
   sights off the left edge and the reticle alone in the middle. The aim's x
   is an absolute PLACE and the frame interpolates to it; both crops now land
   on the same sight picture.
2. The flare's launch point was placed at 17.2° off the lane by arithmetic —
   inside a phone's ~21° half-view — and projected to **ndc.x 1.44**, because
   the shot's own yaw settle turned the lens the other way. And once it was in
   frame it was **seven pixels**, because a 0.5 m ball at 88 m is 0.3°. The
   yaw settle is negative now and the ball is sized by its distance from the
   camera (26–33 px measured across the whole climb).

Also found by the same probe: the burst ramped up from ZERO over 1.1 s, so the
spark went dark for a frame between the climb and the burn. A flare bursts.

**Verified by render** on 1280 × 800 and 390 × 844: hip and aim on both crops,
the film's opening pan, the launch, the climb, the burst, the last frame.
**Verified by measurement**: the flare's NDC position and pixel size at seven
points; the weapon's rest and aim on both crops. **Verified by harness**: the
full suite. `restart` failed once inside a twelve-harness batch on `tallied`
(the five stops' stagger not yet landed when the score roll settled) and
**passes alone in 307 s** — the documented two-at-a-time load flakiness, not a
regression; every other check in that same run was true.

(filled in as they land)

### CP1 · the titles (items 1, 2)

`ep2.title` is **'The 3 AM Shower & The Ghost Cyclist'** — one string, so it
reaches the selector's case card, the episode-complete card's trail, the
chapter card's EPISODE line and the resume note together.

The four card titles drop their second line entirely, because Chad's objection
is to the line itself ("that was not part of the title"), not to its wording:

| chapter | was | now |
|---|---|---|
| e2c1 | The Worst Bed<br>Bed One, Next To The Toilet | The Worst Bed |
| e2c2 | Nobody There<br>Breakfast, The Cookhouse | Nobody There |
| e2c3 | The Pressure<br>Outfield, Night One | The Pressure |
| e2c4 | The Cyclist<br>Outfield, Night Two | The Ghost Cyclist |

Chapter 4's `title` is **The Ghost Cyclist** too — Chad names it that in item 2,
and `title` is what the selector's chapter row shows, so the two must agree.

EPISODE 1 IS UNTOUCHED and that is a judgement, not an oversight: its second
lines are evocative ("The Hell Note<br>I Should Never Have Taken"), not the
location-and-night stamps item 2 objects to. If Chad wants those gone too it is
five more one-line edits.

### CP2 · the four small fixes (items 3, 4, 5, 6)

**Item 3 · the life buoy.** Measured on the shipped set: the walkway's top rail
is at **y 1.00** and the buoy's centre sat at **1.55** — 0.55 m of clear air
above the only thing it could be tied to, which is exactly what Chad saw. The
ring's hole has a radius of 0.18 about its centre, so a centre at **0.86** puts
the rail (0.97–1.03) inside the hole's upper part: the rail passes THROUGH the
ring, which is what hanging on a rail looks like. A lashing strap over the top
closes it.

**Item 4 · the boots and the locker packs.** Gone from the bunk AND from
chapter 2's film pocket, which rebuilds the same room and would otherwise have
contradicted it on screen. The geometries and materials go with them, so
nothing is created that `dispose()` would never reach.

**Item 5 · the twitch, and the legs in the bench.** One clip change caused all
of it, measured on `admintee.glb`:

| take | hips | lowest vertex |
|---|---|---|
| `Sit_and_Doze_Off` | 0.641–0.647 | 0.071 @ z 0.23 |
| `Chair_Sit_Idle_M` | 0.708–0.719 | 0.100 @ z 0.02 |

The cut LIFTED THE MAN 7.0 cm — the twitch — and pulled his feet 21 cm back
under him, into the bench. And a third defect neither of us had seen:
`Chair_Sit_Idle_M`'s head sits 0.52 m over its hips only in its first and last
sixth and **collapses to 0.26** in between (v8.0 measured this and called the
take "not an idle"), so at rate 0.55 any line longer than ~2.9 s had him
folding his head to his knees mid-sentence.

Three defects, one cause, so the cure is to stop changing the clip. `sitUp` now
turns the man's HEAD to the player for the length of his line — chapter 3's
kneeling man (v11.4), with v11.5's `headUndo` so the additive turn cannot stack
— and nudges the doze's rate so he stirs. The API and every call site are
unchanged, so the scenes' timelines did not move.

**Item 6 · "the encik".** The chapter declares an EMPTY `words.approach`, and
the engine now shows the approach prompt only when the chapter has given it
something to say — an empty cell removes the text, which is the sheet's own
rule (docs/EDITING-TEXT.md); an empty floating box is not a removal. Episode 1's
five and the other three episode-2 chapters all declare a word, so none of them
moves.

Harnesses: chapter, walk.

### CP5 · the weapon (items 13, 14, 15) — DONE

**Item 13 · no reload, no ammo count.** `WEAPON_DEFAULTS` gains
`unlimited: false`, so the fixture's nine promises and episode 1 (which
declares no weapon at all) are untouched BY CONSTRUCTION; e2c4 declares
`unlimited: true`. Under it `weaponFire()` skips both the dry-fire gate and
the decrement, `weaponReload()` returns false silently (not with the empty
click — there is nothing on screen asking for a reload), the ammo pill stops
being written, and `<body>` carries `wpnNoAmmo`, which hides `#ammo` and
`#reloadBtn`. `rounds`/`mags` stay DECLARED in the chapter and still ride the
phase string (`moment:r20m2s3`), so the resume format and `applyPhase`'s
regex are byte-identical to v12.3 — they simply never move.

**Item 14 · the AIM button.** 54 px and an SVG crosshair → **82 px and the
word AIM** (76 on the phone, where FIRE is 76), sodium where FIRE is red,
`hud.aim` already being `'AIM'` in the string sheet. No new string.

**Item 15 · the zoom is an animation.** `adsK` (0..1) is stepped on WALL time
over `adsSecs` (0.20 s) inside `weaponFrame`, which already computes a wall
delta — `dt` is clamped to 0.05 s and an aim four times slower on a hot phone
is the v9.3 clock bug in a third place. The lens eases `CAM_FOV → zoom` on a
smoothstep, and `weaponAdsSync` is now only the body class. **The rifle has
NO aiming animation** — the file carries exactly Draw / Shoot / Reload / Hide
(measured, E2-SOLDIER-MODELS §8) — so the second half of the animation is the
weapon itself moving onto the sight line: `adsPos` (−0.12, +0.028, −0.035),
the −0.12 being the body centre's own x at the prop's 0.30 m depth (§8), off
`weaponBase`, the rest `layoutHands` now writes (the torch's `torchBase`
precedent, so a per-frame aim and a per-resize layout cannot fight).
`weaponAdsOff()` zeroes `adsK` as well, so a faint or a teardown cannot leave
the world half-zoomed. **To verify by render at CP10**, with the numbers above
adjusted if the photograph disagrees.

### CP9 · the LOAD drill (item 18) — DONE

**THE CLICK THAT FIRED THE GUN** was real and it was worse than it looked. The
window's own `pointerdown` routes a mouse press to `evPress` (a mouse under
pointer lock never reaches the overlay), and the canvas's `mousedown` fired the
rifle from the SAME click — so every press of the load drill also sent a live
round downrange, which on this range costs six sanity for firing before the
order. `evKey` has said "a live event owns Space / Enter / E" since v7.0 and
nothing ever said it for the mouse; `if (ev) return;` at the top of the canvas
handler says it now, for the trigger and the aim alike. **Mobile was already
safe and that was checked rather than assumed**: `onTouchEnd` only ever reaches
`stage.pile.hits()`, and the FIRE button is hidden under `body.evopen` (v12.2).
Episode 1 declares no events and no weapon, so it cannot reach either half.

**THE DEMO.** `demo` may be `'bar'` as well as `'drag'` — one element, two sets
of children, chosen by the class the briefing puts on it: a bar filling into
the marked band with a tap landing on it, looping, under the title and above
the description, exactly where the standby bed's drag demo sits. A briefing
that declares no demo keeps the element hidden, so every one shipped before
this release is untouched. The FIXTURE declares it on its own sequence event,
so `fixturetest` covers both values rather than only the one the chapter asked
for.

**THE TEXT** is 240 characters down to 122 — the picture says most of it now.

### CP8 · the cyclist (items 20, 21, 22) — DONE

**Item 20 · the banner waits for him, and he arrives on a sting.** The
presence — and with it the red "Something is out there" banner — was raised in
`beginStag` eleven seconds in, EIGHT seconds before the cyclist was on the
range: the HUD announced a presence the player could not find. It is raised on
the frame he is first seen now, with `stingcyc` (bright, 21.9 % above 3 kHz,
no bass at all — a phone plays all of it) and a haptic under it.

**Item 21 · he fades, and he is bigger.** `cycWant` is a target alpha the frame
walks `cyc.a` to at 1.1/s, so a pass fades up and fades away instead of
snapping; a SCENE still sets the alpha outright, because `cycFrame` does not
run under a cutscene and a ramp there would never advance — `cycStart(p,
fadeIn)` defaults to instant for exactly that reason. `PASS_SCALE`
[1.85, 1.55, 1.25, 1.00] scales each pass by how far out it is: a man on a
bicycle 46 m away is 1.2 % of the screen — ten pixels on an 844-tall phone, and
less than half that through the 30° aim — and the pass stopped at the foot of
the berm is left at its own size, which is the size the four scenes were framed
against, so they are untouched by construction.

**And the other half of "may not see it at all"**: a crossing that finished
used to ring the bell at once, so a player who HELD HIS SIGHT — the correct
answer — saw the thing exactly once, and the whole confusion had 14.4 seconds
to happen in. A crossing that finishes with no round fired now takes it into
the trees and brings it back NEARER a moment later; the bell is the moment's
own fifteen-second clock.

**Item 22 · everyone shouts.** On the frame he is seen: his own "That's not a
target." first, because he is the one looking down the lane, then the buddy
(David), the far end (Ronan) and the bunkmate (Kelvin) — Chad's three example
lines in three different voices. It is ONE QUEUE, so every gap is the takes'
own measured length and a re-generated take cannot make two voices talk over
each other (the v9.5 count-off's law); lane five's rifle shot is queued with
them, and `b4there` is dropped from this beat because `r4cyc` says the same
thing in Chad's words and keeping both is the same man shouting it twice. The
take stays in the pack. `beginMoment`'s `b4float` is QUEUED rather than said,
because `sayLine` refuses a line while another speaks and this beat is now
busy — a dropped line is the v8.0 law broken.

### CP7 · the serials (items 17, 19) — DONE

**Item 19 · two serials, not three.** The pop-up serial is gone: statics, then
straight to the mover. Everything the serials do is TABLE-DRIVEN now
(`SER_TARGETS / SER_NEED / SER_KIND / SER_ORDER / SER_LEAD / SER_SECS /
SER_FLARE`), so how many there are is one array length rather than a run of
`if (n === 2)` branches, and the mover's own words (`objS3`) and order
(`t4fire3`) follow it to serial two unchanged. `popSeq` went with the serial it
existed for; `objS2` went with it; `t4fire2` stays in the pack, because
deleting a recorded take to remove a prompt is a trade with no upside (v9.6).

**Item 17 · nothing stands until a wave says so.** Every bank is built DOWN,
`reset()` puts every bank down, `beginLine` puts every bank down (a resume
never plays the film), the FILM puts them back down on its last frame — it
raises the statics for its flare reveal and nothing after it lowered them,
which is exactly the "already standing" Chad reported — `beginSerial` lowers
every bank and the serial's own comes up **on the frame the order lands**, and
`endSerial` lowers every bank rather than only the one just shot. One
consequence worth stating: the two FAR boards at 132 m were standing scenery
and are now down too, because his rule is "all targets", and they are targets.

### CP6 · the e2c4 world (items 9, 10, 16) — DONE

**Item 9 · the live rounds.** Four olive boxes 46 cm long, which at the film's
one-metre shot are four bricks. What a man sees on an ammo table is BRASS:
eighteen rounds standing in a shallow open tray (near real size — 5.56 × 45 is
5.7 mm across and 57 long; these are 9 by 48, a shade fat so they still read on
a phone), brass and metallic under the red lamp that is already the only light
in the shot, four more lying loose beside the tray, four magazines standing in
a row with their feed lips, and two steel ammo boxes with lids, handles and a
stencil. All primitives, no download.

**Item 10 · the encik at the table.** `SAFETY` moves from behind the firing
line to the −x end of the ammo table, which is where a range's safety officer
actually stands — and what the film's second shot has CLAIMED since v12.1
("the safety officer beside it") while he was eleven metres away. His blocker
column is 3.5 m clear of the spawn; `walktest` green (294 s, alone).

**Item 16 · the watchtower.** It was four legs, a deck, two rails and a horn —
a table on stilts. It has a ROOF on four corner posts, a LADDER up the +z face,
a kick plate round the deck, cross-bracing on the two sides the line can see, a
red obstruction light, the range's own red flag on a mast — and a MAN, at the
downrange rail, looking out. He is `fbosling`, which the chapter already parses
for the safety officer, so `loadGltf` costs one `cloneSkinned` and no download,
and `CULL_SPHERE.fbosling` already covers every pose. **The group carries the
deck height rather than a `lift`**, because `mkRig` grounds the model to
`group.position.y` and the primitive proxy rides the group too — with a lift the
placeholder would have stood on the ground under the tower until the bytes
landed.

### CP4 · the e2c4 film (items 8, 12, 11b) — DONE

43.4 s → **35.2 s**, three shots instead of four.

**Item 8** — the TONNER shot is gone. It opened on a strip of dark ground
framed by a tail-gate, which on a portrait phone's centre crop is a black
rectangle with a lamp in the corner, which is exactly his complaint. The film
opens on a PAN across the range instead: from behind the firing line and a
little above it, the lens crossing the lanes and the men already lying on
them, drifting forward and left under the pan so the shot is never a still.

**Item 12** — the fourth shot, back at the ammo table, is gone, and the film
ends on the pan that settles down lane six. One consequence had to be handled:
the old film handed the flare sky back at 36.4 s of a 43.4 s timeline, so a
shorter film would simply have left PLAY under a flare sky — the hand-back is
at 33.4 now, inside the film.

**Item 11 (the film's half)** — the flare is FIRED on screen. `flareUp` no
longer drops a burning light at 46 m: a spark leaves the ground on
`flarelaunch` and climbs for `FLARE_RISE` 1.55 s on `flarePlace(k)`, which
decelerates the way a rocket does, and only at the apex do `flarepop` and the
sky arrive. `flarePlace` is the ONE copy of that arc — the film borrows it,
because `flareFrame` never runs under a cutscene (`updateNotes` returns early),
and the lens rides up on the arc's OWN ease (`k => 1 - (1-k)²`), not a
smoothstep, which would lag at the start and overshoot at the end.

**Where it is fired from is a framing number.** At the first placement
(HIS.x + 5.2, −4.2) the launch sat **36.6°** off his lane, and the 72° lens is
VERTICAL, so a portrait phone's horizontal half-view is about 21° — a phone
player would have heard a launch he could not see, which is the same bug in a
different form. (HIS.x + 2.2, −6.0) is **17.2°**: in frame on a phone, still
clearly off his own lane.

Verified by `chaptertest`, which since v12.3 walks every film and scene against
a recording stub and FAILS on a cue that lands after the last track — the whole
rewritten timeline passes, so no cue is orphaned. The framing itself is
photographed at CP10.

### CP3 · the sound — DONE (items 7, 11a, 20a, 22a)

**ElevenLabs flow `6x6iIs2TXPX9pxCRLAdX`** — six generations, four takes each.
If this session is lost, the takes are still on that flow; poll
`creative_get_flow_run_status` with the flow id and the session ids below, or
just open https://elevenlabs.io/app/flows/6x6iIs2TXPX9pxCRLAdX

| new id | who / kind | words or brief | node |
|---|---|---|---|
| `n3press` (re-said) | jamesTeen · Aaron `B6uUx2p7cRgxseOUyP6P` | `[terrified] Something was pressing against my leg! Not just a sensation — there was actual weight to it!` | `IG8pcv1sqeZMs4cWwr6M` |
| `b4cyc` | buddyC3 · David `XxnXw151E3nb1V85MRlS` | `[shouting, alarmed] Oh my god! What's that?!` | `QjkmyyxNR9NIPi4fJqQj` |
| `k4cyc` | bunkmate · Kelvin `FXMPPfJPpDj0GSwJ6ASO` | `[shouting urgently] There's someone at the range! Don't shoot! DON'T SHOOT!` | `FDkEjo0X1DOBJIrnb3rZ` |
| `r4cyc` | recruit3 · Ronan `xDM73lGN1cPZSG4X87cj` | `[shouting, panicked] Sergeant! There's someone cycling in the range!` | `gYFwetOhL8hfSwbJsEdP` |
| `flarelaunch` | SFX | the thunk of the cartridge leaving the tube, then the climb | `2QSncIpC7pHC5bCQO4bw` |
| `stingcyc` | SFX | the sting on the frame the cyclist is first seen — bright, no bass | `liKtY0EJd3jIUtdA6lQ7` |

The three shouts are Chad's own example lines (item 22), cast on three
DISTINCT voices — `recruit4` was passed over because it shares David's voice id
with the buddy, and "different voicelines" has to mean different voices too.
The sting is briefed with **almost no low bass** on purpose: v10.4's law, that
a phone speaker reproduces almost nothing under 120 Hz, so a sub-bass sting is
a sting Chad cannot hear.

`flarepop` and `flarehiss` already exist and stay — item 11's missing half is
the LAUNCH, both the sound and the thing in the sky.

**WHAT LANDED.** The first run's session ids were never written down and the
status tool needs them, so the six nodes were RE-RUN and the ids written to
`masters/v13.0/sessions.json` before anything else. **The session ids are the
only handle on a generation** — write them the moment a run starts. Three of
the 24 takes failed (eleven_v3 fails about one line in three, at random — the
v4.1 law: do NOT rewrite a failed line), leaving 2 takes for `k4cyc` and 3 for
`r4cyc`, which is enough to pick from.

The picks and the reason for each are in `masters/v13.0/make.sh`, measured by
`measure.py` into `measure.json`. Installed lengths: `n3press` 5.64 → **6.35**,
`b4cyc` 2.12, `k4cyc` 3.42, `r4cyc` 2.35, `flarelaunch` 1.07, `stingcyc` 1.49.
Levels as v12.1: Aaron peak-matched −6.85 mp3 / −6.6 ogg, the cast by RMS to
−16 dBFS (the flat bus), effects −4.0 peak, each encoder iterated to its own
target (the v9.6 per-file delta law).

Wired: three registry rows + `n3press`'s new text and secs in
`src/voicelines.js`; five `STING_SAMPLE` rows and three `CAST_TAKES` names in
`src/main.js`; `SECS.n3press` in e2c3, where the beat after the line is stated
AS `SECS.n3press`, so it moved with the take and nothing had to be re-timed.
The cues themselves are CP4's (the flare) and CP8's (the sting and the shouts).

**All four takes of every line are kept in `masters/v13.0/raw/`** — if Chad
wants a different read, it is a one-line change in `make.sh` and a re-run.
