# v10.3 · The door, the dread, and the bed that waits

Chad's twelve notes on episode 2 chapter 1 (The Worst Bed), 14 Sep 2026, and
what shipped for each. `src/main.js` changes three things, all absent from
episode 1 by construction (no zero delta, no heartbeat, no briefing there).

| # | Chad | shipped |
|---|---|---|
| 1 | "20 of us" → nine | `n1pro2` re-said by Aaron (take 2 of 2: the flatter read, tail −42 dBFS where take 1 ended on signal), 6.84 s; the card's brief says "Nine recruits" too |
| 2 | last line → "I told myself it didnt matter where you slept....but I was wrong...." | `n1pro4` re-said (take 2: flat, head −47 / tail −46, the one real 0.18 s pause at "slept... but"), 4.68 s; cued at 71.6 as before |
| 3 | the eerie ambient much louder, film + play + outcomes | `e2dread` 0.26 → 0.62 in `mixBeds` — it is the one bed keyed to nothing (v9.7), so one number covers all three |
| 4 | end the film peeking at the toilet, the ghost's silhouette barely visible through the small opening | PEEK = his pillow, head 15 cm toward the door (photographed: at z 3.0 the bunk's post stood in the opening); yaw onto the man in the shower placed for the film at (−0.85, 6.15), inside the wedge between the ajar leaf and the far jamb, back to the door; alpha 0 → 0.34 over 75.6–79.0; the last step puts him back at `WC_GHOST` |
| 5 | the echoey "ten" even louder | `c1ten` rebuilt from `masters/v9.5/ten_a.mp3` with the v9.7 treatment (pitch ×0.9, lowpass 3.4 kHz, echo taps 90/210/380/620 ms), peak −5.0 mp3 / −4.8 ogg against c1nine's −7.23; recipe in `masters/v10.3/make.sh` |
| 6 | a looping drag animation under the STANDBY BED title | engine: `kit.event({ demo: 'drag' })` shows `#evDemo` while briefing (CSS keyframes: a tile from the left slot to the right, a fingertip with it, the target slot glowing on arrival, 2.6 s loop); hidden by `evBegin`; the fixture's match declares it |
| 7 | encik faces the player for the toggle-rope line | `faceRigAt(encik, yaw.position.x, yaw.position.z, 0.6)` 0.7 s before `e1rope`; ry = atan2(dx, dz) is "face the target" for these rigs (ry 0 faces +z); measured −0.427 = −0.427, photographed square on |
| 8 | ghost fades the moment he is seen; faces the shower tap, back to the player | `ghostWCShow(false, 1.4)` on entry (was: after his line + 0.5); `ry: WC_FACE_TAP` = atan2(0, 1.27) = 0 |
| 9 | no E on the bed until the shower scene and the heartbeat are done; the menu opens itself; E only recalls it | `bedWants()` answers `BED_DECIDE` only in phase `decide` (was `night || decide`); the heartbeat's completion already sets `decide` and opens the decision 0.4 s later; Escape + E reopens. Measured: inView false / press refused through the fear; state `decide` after; reopened on the press |
| 10 | the tap ring not aligned to the inner circle | `evBurst` positions itself from `#evRing`'s own offset box when the ring is displayed (centres 450/241.5 both, the x wobble is the miss animation's shake) |
| 11 | first option shows no sanity bar | engine: `statRowsHTML` filters `k in d` instead of a truthy delta — option A's sanity delta is 0 and was dropped; the row now reads +0 |
| 12 | blanket: fade to black under "it's nothing", then the card | `scBlanket` is 14.0 s: `fade(1.0, 2.4, 0, 1)` under the blanket sound, the two lines at 2.8 and 7.2, the shower and the presence climbing, `boom` at 11.6, black held to the card; the weave overlay is not raised |

Verified on the hosted build with `dbg-v103.mjs` / `dbg-v103b.mjs` /
`dbg-v103c.mjs` (gitignored) and the chapter, cine, fixture, walk, text,
state, leak, menu and restart harnesses. Sheet v48 exported to
`masters/v10.3/masterz-text-v48.xlsx` (not published — the base64 wall,
docs/EDITING-TEXT.md); v44 remains the link.
